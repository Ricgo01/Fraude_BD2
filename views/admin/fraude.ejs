<%- include('../partials/head', { title }) %>
<%- include('../partials/navbar', { title, role }) %>

<div class="layout">
  <%- include('../partials/sidebar', { role }) %>
  <main class="content">
    <h1>Detección de Fraude</h1>
    <p>Aquí se mostrarán cuentas compartidas, documentos reutilizados y redes de fraude.</p>
  </main>
</div>

<%- include('../partials/footer') %>